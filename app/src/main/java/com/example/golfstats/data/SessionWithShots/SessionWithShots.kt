package com.example.golfstats.data.SessionWithShots


/*
import ShotRow
import androidx.room.Embedded
import androidx.room.Relation
import com.example.golfstats.data.Sessions.SessionRow

data class SessionWithShots(
    @Embedded
    val session: SessionRow,
    @Relation(
        parentColumn = "id",
        entityColumn = "session_id"
    )
    val shots: List<ShotRow>
)*/